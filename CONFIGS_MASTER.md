# CONFIGS MASTER — Hydragon2000's NixOS system

> **Source of truth**: `/etc/nixos/` (Nix flake + Home Manager)
> All user-level configs under `~/.config/` are *generated* or *templated* from
> `home.nix` by Home Manager. **Edit the flake, not the runtime files.**
> Runtime files (`~/.config/waybar/config.jsonc`, `~/.config/hypr/hyprland.conf`, etc.)
> get overwritten on `nixos-rebuild switch` — so edit `/etc/nixos/home.nix` instead.

---

## A. SYSTEM-LEVEL CONFIG — `/etc/nixos/` (Nix Flake)

| File | Purpose | Notes |
|---|---|---|---|
| `/etc/nixos/flake.nix` | Flake entry point; defines `hostname` + `username` vars, passes them to all modules via `specialArgs` | **Edit hostname/username here on new machine** |
| `/etc/nixos/configuration.nix` | System config: bootloader, networking, GPU, Hyprland, PipeWire, SDDM autologin | GPU section marked `MACHINE-SPECIFIC` — edit per hardware |
| `/etc/nixos/hardware-configuration.nix` | Auto-generated hardware config (generated by `nixos-generate-config`) | Read-only; regenerated on each machine. In `.gitignore` |
| `/etc/nixos/home.nix` | **User config**: git, SSH, GTK theme, cursor, Zsh, Hyprland, Waybar, all packages | Uses `{ username, hostname }` variables from flake |
| `/etc/nixos/theme.nix` | Theme system: waybar colors + cursor theme. Has 3 themes, `default` and `bibata-classic` are duplicates | **Duct tape** — duplicate themes, AI-generated and never deduped |
| `/etc/nixos/waybar-config.jsonc` | Waybar layout JSON, sourced into `~/.config/waybar/config.jsonc` by home.nix | Edit this if you want waybar changes (not the runtime file) |
| `/etc/nixos/flake.lock` | Pinned dependency versions | Auto-generated |
| `/etc/nixos/repo/` | Git repo — contains all files above + `README.md` + `sync.sh` + `gen-commit-msg.py` | **The portable flake — clone this for a fresh install** |
| `/etc/nixos/repo/README.md` | Install instructions for a fresh NixOS system | Covers partitioning, install, and per-machine customization |

---

## B. RUNTIME CONFIGS — `~/.config/` (Mostly HM-managed)

### Window Manager / Compositor
| File | Source of Truth | Purpose |
|---|---|---|
| `~/.config/hypr/hyprland.conf` | `/etc/nixos/home.nix` (inline text) | Hyprland keybinds, monitors, rules. **Edit home.nix, not this file** — it gets overwritten |
| `~/.config/waybar/config.jsonc` | `/etc/nixos/waybar-config.jsonc` | Waybar modules/layout |
| `~/.config/waybar/style.css` | `/etc/nixos/home.nix` (inline text w/ theme vars) | Waybar colors (uses `${theme.current.*}` variables) |
| `~/.config/waybar/config.jsonc.hm-backup` | *backup* | **Redundant** — HM backup from before first overwrite. Can delete |
| `~/.config/waybar/style.css.hm-backup` | *backup* | **Redundant** — HM backup. Can delete |

### Terminal Emulators
| File | Source of Truth | Purpose |
|---|---|---|
| `~/.config/alacritty/alacritty.toml` | Runtime (not HM-managed) | Window padding (x=8, y=8). Minimal |
| `~/.config/kitty/` (empty) | N/A | **Unused** — kitty is installed but never configured. Empty dir |
| `~/.config/btop/btop.conf` | Runtime | System monitor config. Full default with minor tweaks |
| `~/.config/btop/themes/` (empty) | N/A | **Unused** — no custom themes placed here |

### Shell / Prompt
| File | Source of Truth | Purpose |
|---|---|---|
| `~/.zshenv` | /etc/nixos/home.nix | Sources HM session vars. **Has an empty if-guard** — duct tape |
| `~/.zshrc` | /etc/nixos/home.nix (via `initContent` + shellAliases) | History opts, Starship/Zoxide/Fzf init, aliases (cat=bat, ls=eza, rebuild, drybuild) |
| `~/.zsh_history` | Runtime | Zsh history |
| `~/.config/starship.toml` | Runtime | Minimal prompt config (dir+git+character) |
| `~/.zcompdump` | Auto-generated | Zsh completion dump. Safe to ignore |

### Git / GitHub
| File | Source of Truth | Purpose |
|---|---|---|
| `~/.config/git/config` | `/etc/nixos/home.nix` | Git user (dondragonstar) + `includeIf` for professional gitdir |
| `~/.gitconfig-professional` | `/etc/nixos/home.nix` (via `home.file`) | Professional git identity (DevaJ2005) + SSH URL rewrite |
| `~/.config/gh/config.yml` | Runtime | GitHub CLI config (https protocol, aliases) |
| `~/.config/gh/hosts.yml` | Runtime (auto-login) | **Contains OAuth token** in plaintext. Auth artifact |

### SSH
| File | Source of Truth | Purpose |
|---|---|---|
| `~/.ssh/config` | `/etc/nixos/home.nix` (via `programs.ssh.matchBlocks`) | Two hosts: `github.com` (personal key) + `github-professional` (pro key) |

### Theme / GTK / Cursor
| File | Source of Truth | Purpose |
|---|---|---|
| `~/.gtkrc-2.0` | `/etc/nixos/home.nix` (via `gtk.*`) | GTK2 theme (Catppuccin, Papirus, Bibata cursor) |
| `~/.config/gtk-3.0/settings.ini` | `/etc/nixos/home.nix` | GTK3 theme (same as GTK2) |
| `~/.config/gtk-3.0/settings.ini.hm-backup` | *backup* | **Redundant** — old GTK3 settings. Can delete |
| `~/.config/gtk-4.0/settings.ini` | `/etc/nixos/home.nix` | GTK4 theme (cursor + icon, no theme name — bug?) |
| `~/.config/fontconfig/conf.d/10-hm-fonts.conf` | `/etc/nixos/home.nix` (auto) | Nix profile fonts |
| `~/.config/fontconfig/conf.d/52-hm-default-fonts.conf` | `/etc/nixos/home.nix` (auto) | Default fonts — **empty config block** (duct tape placeholder) |

### Desktop / App-Specific
| File | Source of Truth | Purpose |
|---|---|---|
| `~/.config/rofi/config.rasi` | Runtime | Rofi launcher — just imports solarized theme from nix store |
| `~/.config/Thunar/uca.xml` | Runtime | Custom action: "Open Terminal Here" |
| `~/.config/Thunar/accels.scm` | Runtime | Keyboard shortcuts (all commented = defaults) |
| `~/.config/zed/settings.json` | Runtime | Zed editor settings (agent font, dark theme, telemetry off) |
| `~/.config/vlc/vlcrc` | Runtime | VLC config — **4666 lines, all commented out**. Default autogenerated |
| `~/.config/discord/settings.json` | Runtime | Discord window bounds, SKIP_HOST_UPDATE |
| `~/.config/pavucontrol.ini` | Runtime | Audio mixer window state |
| `~/.config/mimeapps.list` | Runtime | Default apps: Telegram for tg://, Firefox/Zed for HTML |
| `~/.config/herdr/config.toml` | Runtime | herdr AI terminal: agent panel sort = spaces, theme = terminal |
| `~/.config/waypaper/config.ini` | Runtime | Wallpaper manager (backend=swww, wp dir, transition settings) |
| `~/.config/waypaper/config.ini.hm-backup` | *backup* | **Redundant** — old waypaper config. Can delete |

### System Services
| File | Source of Truth | Purpose |
|---|---|---|
| `~/.config/systemd/user/tray.target` | Auto | HM System Tray target |
| `~/.config/environment.d/10-home-manager.conf` | Auto | LOCALE_ARCHIVE env var |
| `~/.config/dconf/user` | Auto | GNOME/GTK color-scheme = prefer-dark |
| `~/.config/dconf/.keep` | Auto | Placeholder |

### opencode / AI
| File | Source of Truth | Purpose |
|---|---|---|
| `~/.config/opencode/opencode.jsonc` | Runtime | Auto-loads CONFIGS_MASTER.md as instruction; permissions allow sync.sh + git without prompting |
| `~/.config/opencode/plugins/herdr-agent-state.js` | Runtime | herdr integration plugin — syncs agent state to herdr pane |
| `~/CONFIGS_MASTER.md` | **THIS FILE** | Master lookup table for all configs |

---

## C. DUCT TAPE / AI-BUILT-AND-NEVER-REFACTORED ITEMS

1. **`/etc/nixos/theme.nix`**: 3 themes where `default` and `bibata-classic` are identical. The `google-dot` theme only differs in cursor name. Clearly AI-generated once and never cleaned up.

2. **`~/.zshenv`**: Has `if [[ -z "${__HM_ZSH_SESS_VARS_SOURCED-}" ]]; then export __HM_ZSH_SESS_VARS_SOURCED=1; fi` with literally nothing inside the if block. The guard exists but guards nothing.

3. **`/etc/nixos/home.nix` comment on Hyprland**: Says "Minimal placeholder Hyprland config so the compositor starts with *something* on first login instead of a blank/black screen. You will replace this with your own dotfiles once booted." — but this IS the production config now. The comment is misleading.

4. **`~/.config/fontconfig/conf.d/52-hm-default-fonts.conf`**: Valid XML with an empty `<fontconfig>` block. Does nothing.

5. **`~/.config/vlc/vlcrc`**: 4666 lines of all-commented-out defaults. VLC creates this on first launch and never prunes it.

6. **Waybar config duplication**: The source config is in `/etc/nixos/waybar-config.jsonc`, but it gets copied to `~/.config/waybar/config.jsonc`. The runtime file has slightly different formatting (no trailing commas) — meaning someone edited the runtime file after deployment. On next `nixos-rebuild`, those changes vanish.

7. **`/etc/nixos/repo/` vs `/etc/nixos/`**: NixOS configs exist in TWO places — `/etc/nixos/` (active) and `/etc/nixos/repo/` (git repo copy). This invites drift.

---

## D. REDUNDANT / UNUSED / SAFE-TO-DELETE

| File | Reason |
|---|---|
| `~/.config/waybar/config.jsonc.hm-backup` | HM backup, current file is live |
| `~/.config/waybar/style.css.hm-backup` | HM backup, current file is live |
| `~/.config/gtk-3.0/settings.ini.hm-backup` | HM backup, current file is live |
| `~/.config/waypaper/config.ini.hm-backup` | HM backup, current file is live |
| `~/.config/kitty/` (empty dir) | No config placed here |
| `~/.config/nvim/` (doesn't exist) | Neovim installed but never configured |
| `~/.config/nautilus/` (empty dir) | Nautilus installed but never configured |
| `~/.config/btop/themes/` (empty dir) | No custom themes |
| `~/.config/fontconfig/conf.d/52-hm-default-fonts.conf` (empty config) | Does nothing |

---

## E. CONFIG HIERARCHY (HOW THINGS CONNECT)

```
/etc/nixos/flake.nix
  ├── /etc/nixos/configuration.nix     (system: boot, GPU, Hyprland, PipeWire, SDDM)
  ├── /etc/nixos/hardware-configuration.nix  (auto-generated)
  └── home-manager
        └── /etc/nixos/home.nix         (user: git, SSH, GTK, Zsh, packages)
              ├── /etc/nixos/theme.nix  (theme vars → waybar colors, cursor)
              ├── /etc/nixos/waybar-config.jsonc  (copied → ~/.config/waybar/)
              └── inline text blocks → various ~/.config/ files
```

**Golden rule**: Edit `/etc/nixos/home.nix` for user-level changes, `/etc/nixos/configuration.nix` for system-level. **AI agents must NOT run nixos-rebuild** — the human handles that after reviewing changes.

---

## F. QUICK COMMANDS

```bash
# Rebuild after editing configs
sudo nixos-rebuild switch --flake /etc/nixos#hydragon2000-pc

# Dry build (preview without applying)
sudo nixos-rebuild dry-build --flake /etc/nixos#hydragon2000-pc

# Aliases defined in ~/.zshrc
rebuild    → sudo nixos-rebuild switch --flake /etc/nixos#hydragon2000-pc
drybuild   → sudo nixos-rebuild dry-build --flake /etc/nixos#hydragon2000-pc
cat        → bat
ls         → eza
```

---

## G. THE SYNC LOOP (CRITICAL — AI AGENTS MUST FOLLOW THIS)

The NixOS configs live in two places:
- **`/etc/nixos/`** — the *live* configs NixOS actually reads
- **`/etc/nixos/repo/`** — a git repo (`origin: git@github.com:dondragonstar/my-nix-install-helper.git`)

They **will diverge** if not synced. The sync script does:
```
/etc/nixos/  ──copy──→  /etc/nixos/repo/  ──commit──→  git push origin main
```

### Mandatory workflow for EVERY AGENT SESSION

```
1. Edit config files under /etc/nixos/ or ~/CONFIGS_MASTER.md
2. Run /etc/nixos/repo/sync.sh
   → copies changed files into repo, commits, pushes to origin/main
   → permitted without prompting by opencode's permission config
```

The script handles:
- `configuration.nix`, `flake.nix`, `flake.lock`, `home.nix`, `theme.nix`
- `waybar-config.jsonc`, `alacritty.toml`, `CONFIGS_MASTER.md`
- Excludes `hardware-configuration.nix` (auto-generated)

### AI commit messages (NEW — 2026-07-05)

`sync.sh` now pipes the actual git diff to `qwen2.5-coder:3b` (local Ollama) to generate
**meaningful commit messages** describing what changed and why. No more `auto-sync: date` timestamps.

The flow:
```
edit config → sync.sh → copies files → git diff | ollama → AI writes commit msg → push
```

- `sync.sh:41-43` — sends diff to Ollama; falls back to stat-based msg if AI fails
- `gen-commit-msg.py` — Python helper that calls Ollama API, lives in repo dir
